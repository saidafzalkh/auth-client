import { Container } from "react-bootstrap";

const Error = () => {
  return (
    <Container>
      <main className="d-flex vh-100 align-items-center justify-content-center flex-column">
        <h1>OOPS!</h1>
        <i>Error :(</i>
      </main>
    </Container>
  );
};

export default Error;
